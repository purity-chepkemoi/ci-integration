**CI Integration Workflow**

This project demonstrates a basic GitHub Actions workflow.

**Features**

- Manual workflow trigger
- Runs on Ubuntu runner
- Prints a message to the console

**Workflow Trigger**

The workflow runs manually using `workflow_dispatch`.

**Output**

The pipeline prints:

"this is our first task"
